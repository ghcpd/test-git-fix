[pytest]
# Document the custom 'network' marker so runs that inspect markers
# do not receive an 'unknown marker' warning.
markers =
    network: marks tests that require outbound network access and may be skipped in isolated environments

# Leave default test discovery alone; maintainers can use -k 'not network'
# to exclude network tests in isolated packaging environments.